{
  "name": "@shared/providers",
  "version": "0.0.1",
  "description": "Uman Common utils collections",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "prepublish": "tsc --composite false",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc --composite false",
    "test": "jest",
    "test:update": "jest -u",
    "test:ci": "jest --runInBand",
    "test:watch": "jest --watch",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx",
    "clean-install": "rimraf node_modules && yarn"
  },
  "author": "Chirag Rami",
  "license": "MIT",
  "dependencies": {
  },
  "devDependencies": {
  },
  "peerDependencies": {
    "react": "*",
    "react-native": "*"
  }
}
